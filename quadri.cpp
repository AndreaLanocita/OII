// comincio in un for a sommare uno dopo l'altro i valori nel vettore
// quando supero massimale salvo la variabile del for come possibile situazione
// controllo se funziona per ogni altro elemento del vettore dal 2 in poi

// ogni volta che scorro il vettore tengo la stessa somma di valori dei quadri, 
// solo togliendo il valore dell'elemento non più incluso e aggiungendo quello dopo

// se il valore va oltre a quello limite durante il controllo, salvo il nuovo valore
// e continuo

// in pratica, all'inizio due for l'uno dentro l'altro fin quando trovo soluzione parziale
// poi un singolo for che va avanti

int quadri(int N, long long M, int V[]) {
    // Scrivete qui la vostra soluzione
    int B = 42;
    return B;
}
