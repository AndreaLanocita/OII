#include <bits/stdc++.h>

using namespace std;

// input data
int A, B;
long long K, grandezza;
deque <long long> answer;

int main() {
//  uncomment the following lines if you want to read/write from files
//  ifstream cin("input.txt");
//  ofstream cout("output.txt");

    cin >> A >> B >> K;
    answer.push_front(K);

    while(K != 0) {
        
    }

    cout << answer << endl; // print the result
    return 0;
}
